import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    width: "100%",
    borderColor: "black",
    borderWidth: 2,
    borderRadius: 5,
    height: 100,
    marginTop: 15,
  },
});

export default styles;
